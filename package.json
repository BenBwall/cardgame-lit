{
  "name": "@benbwall/cardgame-lit",
  "version": "1.0.0",
  "description": "A local single-player card table, ported from BenBwall/cardgame to Lit.",
  "files": [
    "dist",
    "licenses",
    "CARD-ART-LICENSES.md",
    "README.md"
  ],
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc && bun scripts/build-package.ts && bun scripts/build-demo.ts",
    "check": "tsc --noEmit && tsc --project server/tsconfig.json && tsc --project tests/tsconfig.json",
    "server": "bun server/main.ts",
    "server:dev": "bun server/main.ts --dev",
    "test": "bun test tests",
    "test:browser": "bun run build && bun --bun playwright test",
    "dev": "bun scripts/preview.ts --dev",
    "railway:plan": "railway config plan --file railway-backend.ts",
    "railway:apply": "railway config apply --file railway-backend.ts"
  },
  "dependencies": {
    "@lucide/icons": "^1.46.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/bun": "^1.4.2",
    "lit": "^3.3.3",
    "railway": "3.11.0",
    "typescript": "^6.0.3"
  },
  "peerDependencies": {
    "lit": "^3.3.3"
  },
  "packageManager": "bun@1.4.2"
}
